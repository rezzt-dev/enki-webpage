---
/** Contenedor horizontal canónico de la landing. */
interface Props {
  wide?: boolean;
  class?: string;
}

const { wide = false, class: className } = Astro.props;
---

<div class:list={["mx-auto px-4 md:px-6", wide ? "max-w-[1350px]" : "max-w-[1200px]", className]}>
  <slot />
</div>
