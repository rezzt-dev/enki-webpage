---
/** Capa de grano decorativa y no interactiva. */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["grain pointer-events-none fixed inset-0 z-[60]", className]} aria-hidden="true"></div>
